import 'package:backgrounds/src/widgets/header_cuadrado.dart';
import 'package:flutter/material.dart';

class HeaderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // body: HeaderCuadrado(),
      // body: HeaderCircular(),
      // body: HeaderDiagonal(),
      // body: HeaderTriangulo(),
      // body: HeaderPico(),
      // body: HeaderCurvo(),
      body: HeaderWave(),
    );
  }
}
